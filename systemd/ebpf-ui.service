[Unit]
Description=eBPF Dashboard
After=network.target ebpf-collector.service

[Service]
Type=simple
User=abhishek
WorkingDirectory=/home/abhishek/ebpf-microseg/ui
ExecStart=/bin/bash -c 'source venv/bin/activate && streamlit run app.py --server.port 8501 --server.headless true'
Restart=always

[Install]
WantedBy=multi-user.target
